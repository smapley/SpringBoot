server:
  port: 7070 #端口

spring:
  datasource: #数据库配置
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

  jpa.properties.hibernate.hbm2ddl.auto: create-drop #自动更新表